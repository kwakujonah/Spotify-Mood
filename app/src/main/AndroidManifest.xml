<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kwakujonah.spotifymooder">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpotifyMooder">
        <activity
            android:name=".ListsActivity"
            android:exported="false"
            android:label="@string/title_activity_lists"
            android:theme="@style/Theme.SpotifyMooder.NoActionBar" />
        <activity
            android:name=".MyMoodActivity"
            android:exported="true"
            android:label="@string/title_activity_my_mood"
            android:theme="@style/Theme.SpotifyMooder.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>